import{requireAdmin}from"@/lib/admin-auth";import{dateRange,reportData}from"@/lib/admin-data";import{formatPrice}from"@/lib/format";import{ReportFilters}from"@/components/admin/ReportFilters";export default async function Best({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){await requireAdmin("reports.view");const q=await searchParams,{start,end}=dateRange(q.period,q.from,q.to),d=await reportData(start,end),total=d.best.reduce((s,x)=>s+Number(x.units),0);return <><div className="page-head"><div><h1>محصولات پرفروش</h1><p>بر پایه اقلام سفارش‌های غیرلغوشده</p></div></div><ReportFilters period={q.period}/><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>رتبه</th><th>محصول</th><th>SKU</th><th>تعداد فروش</th><th>سهم</th><th>درآمد</th><th>موجودی</th></tr></thead><tbody>{d.best.map((x,i)=><tr key={`${x.product_id}-${x.sku}`}><td>{(i+1).toLocaleString("fa-IR")}</td><td><strong>{x.name}</strong></td><td>{x.sku}</td><td>{x.units}</td><td>{total?Math.round(x.units/total*100):0}%</td><td>{formatPrice(Number(x.revenue))}</td><td><span className={`badge ${x.stock===0?"danger":""}`}>{x.stock??"—"}</span></td></tr>)}</tbody></table></div></>}

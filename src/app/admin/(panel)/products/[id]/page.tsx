import {notFound} from "next/navigation";import {eq} from "drizzle-orm";import {db} from "@/db";import {categories,products} from "@/db/schema";import {requireAdmin} from "@/lib/admin-auth";import {ProductForm} from "@/components/admin/ProductForm";export default async function EditProduct({params}:{params:Promise<{id:string}>}){await requireAdmin("products.edit");const id=Number((await params).id);const [[product],cats]=await Promise.all([db.select().from(products).where(eq(products.id,id)).limit(1),db.select({id:categories.id,name:categories.name}).from(categories)]);if(!product||product.deletedAt)notFound();return <><div className="page-head"><div><h1>ویرایش محصول</h1><p>{product.name} · {product.sku}</p></div></div><ProductForm categories={cats} initial={product}/></>}

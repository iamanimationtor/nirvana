import{requireAdmin}from"@/lib/admin-auth";import{dateRange,reportData}from"@/lib/admin-data";import{formatPrice}from"@/lib/format";import{ReportFilters}from"@/components/admin/ReportFilters";export default async function Reports({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){await requireAdmin("reports.view");const q=await searchParams,{start,end}=dateRange(q.period,q.from,q.to),d=await reportData(start,end),s=d.summary;const net=Number(s.revenue)-Number(s.refunds);return <><div className="page-head"><div><h1>گزارش مالی</h1><p>درآمد، تخفیف و بازپرداخت؛ سود فقط پس از ثبت هزینه قابل محاسبه است.</p></div><a className="btn btn-primary" href={`/api/admin/reports/export?from=${start.toISOString()}&to=${end.toISOString()}`}>دریافت CSV</a></div><ReportFilters period={q.period}/><div className="admin-table-wrap"><table className="admin-table"><tbody><tr><th>فروش ناخالص</th><td>{formatPrice(Number(s.gross_sales))}</td></tr><tr><th>تخفیف</th><td>{formatPrice(Number(s.discounts))}</td></tr><tr><th>درآمد ارسال</th><td>{formatPrice(Number(s.shipping))}</td></tr><tr><th>درآمد پرداخت‌شده</th><td>{formatPrice(Number(s.revenue))}</td></tr><tr><th>بازپرداخت</th><td>{formatPrice(Number(s.refunds))}</td></tr><tr><th>فروش خالص پس از بازپرداخت</th><td><strong>{formatPrice(net)}</strong></td></tr><tr><th>سود</th><td>قابل محاسبه نیست — داده هزینه کالا ثبت نشده است</td></tr></tbody></table></div></>}

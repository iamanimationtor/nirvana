import{pool}from"@/db";import{requireAdmin}from"@/lib/admin-auth";import{CategoryCreate}from"@/components/admin/CategoryCreate";export default async function Categories(){await requireAdmin("products.view");const r=await pool.query("SELECT c.*,COUNT(p.id)::int products FROM categories c LEFT JOIN products p ON p.category_id=c.id AND p.deleted_at IS NULL GROUP BY c.id ORDER BY c.sort_order,c.name");return <><div className="page-head"><div><h1>دسته‌بندی‌ها</h1><p>ساختار کاتالوگ فروشگاه</p></div></div><div className="admin-grid-2"><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>نام</th><th>نشانی</th><th>محصولات</th><th>وضعیت</th></tr></thead><tbody>{r.rows.map(x=><tr key={x.id}><td>{x.icon} <strong>{x.name}</strong></td><td dir="ltr">{x.slug}</td><td>{x.products}</td><td><span className={`badge ${x.active?"success":""}`}>{x.active?"فعال":"غیرفعال"}</span></td></tr>)}</tbody></table></div><CategoryCreate/></div></>}
